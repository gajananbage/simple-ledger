[project]
name = "simple_ledger"
version = "1.0.0"
description = "A Secure and Simple Ledger Application built with Python Flet"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flet>=0.85.1",
]

[tool.flet]
app-name = "Simple Ledger"
main-module = "main"

[tool.flet.android]
package-name = "com.yourname.simple_ledger"
version-code = 1
version-name = "1.0.0"
